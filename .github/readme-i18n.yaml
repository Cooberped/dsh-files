# Bilingual-pair consistency record: git blob hashes of each side as of the
# last confirmed-consistent state. After editing either side, bring the other
# along and re-record with `git hash-object <file>`.
#
# Kept outside the repository root because npm force-includes README* into the
# published tarball regardless of the package.json "files" allowlist, and this
# is a development-only record that users have no reason to install.
README.md: 4082c4271a2bc6703b6602cd037a882e75300212
README.zh.md: ca6615a14bdeaada40d8ad88c1fd73b9245033a4
